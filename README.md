# Testing OpenAI

A tiny Python greeting app with multilingual support.

## Usage

Run the script directly:

```bash
python main.py
```

Provide a name or language code for a customized greeting:

```bash
python main.py Alice --language en
python main.py Carlos -l es
```
